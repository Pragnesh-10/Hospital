import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { Suspense } from "react";
import { MessageToast } from "@/components/shared/MessageToast";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MediCare Plus | Advanced Hospital Management System",
  description: "A premium, modern hospital management system offering world-class healthcare services, instant appointment booking, and patient records.",
  keywords: "hospital, healthcare, booking, appointment, medical, doctors",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Toaster position="top-right" />
        <Suspense fallback={null}>
          <MessageToast />
        </Suspense>
      </body>
    </html>
  );
}
